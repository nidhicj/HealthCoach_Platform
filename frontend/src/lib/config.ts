// Empty string: all browser calls are same-origin, proxied by app/api/[...path]/route.ts.
// NEXT_PUBLIC_API_URL is consumed server-side only (the Route Handler reads it for BACKEND).
export const API_URL = "";
