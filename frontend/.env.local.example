# Backend URL consumed by app/api/[...path]/route.ts (BFF proxy).
# In dev: Next.js dev server proxies /api/* to this URL server-to-server.
# In production: set NEXT_PUBLIC_API_URL in Secret Manager (baked at Docker build time).
NEXT_PUBLIC_API_URL=http://localhost:8000
